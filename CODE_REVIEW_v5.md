# zahyou v5 コードレビューと v6 の変更点

対象: `zahyou.ipynb`（`origin/main` = tag `v5`, commit `cafdf3e`「オフライン解析機能を追加」）
テスト画像: `Downloads/Capture_00001 00_10_33.fits`（ZWO ASI290MM / SharpCap 4.1 / 8bit /
968×548 / bin2 / 露出 0.0711 秒）
目標: `UCAC4 660-021020`（RA 61.911992°, Dec 41.986262° / G=13.06）

---

## 1. 結論 —— オフライン解析が失敗していた原因

**`convert_to_safe_png()` の 1〜99 パーセンタイル引き伸ばしが、星を全部つぶしていた。**

星野写真では明るい画素が画像全体の 1% に満たない。したがって 99 パーセンタイルは
「明るい星」ではなく **ノイズの裾** に落ちる。テスト画像で実測すると:

| 量 | 値 |
|---|---|
| 引き伸ばし窓 `np.percentile(data, [1, 99])` | **5 〜 92 ADU** |
| 実際に写っている星のピーク（16 個） | **155 〜 217 ADU** |
| → 白飛びした星 | **16 / 16 個（全部）** |
| 8bit 変換後の背景ノイズ σ | **49.8 / 255 階調** |
| 8bit 変換後に星が背景から出られる最大の高さ | **152 階調**（= 3.0σ 相当） |

元の FITS では星の SNR は 10σ 以上あるのに、PNG にした時点で **上限 3σ** まで落ちる。
しかも星は全部 255 に張り付くので **明るさの順序も PSF の形も消える**。
astrometry.net の `simplexy` は「明るい順に N 個」を取って四辺形を作るので、
この状態では *ノイズの塊と焼き込み文字* を星だと思って quad を作り、当然マッチしない。

実測での裏取り:

```
元の FITS        : 5σ 以上の検出 62 個（うち実星 16 個）
v5 の PNG        : 5σ 以上の検出  0 個   ← 何も見つからない
```

そして同じ画像を **そのまま** nova.astrometry.net に投げると **17 秒で解ける**。
つまりエンジンでもインデックスでもなく、**前処理が原因**だった。

### 追い打ちになっていた要因

2. **焼き込みタイムスタンプ**。この画像の上端 0〜16 行に
   `2025 09 19 15:10:33:248 Z` が焼き込まれている。文字はピーク 217 ADU で
   実星より明るく、しかも 1 行に数十個並ぶので、
   「明るい順」で並べると上位を文字が占領する。
3. **`--scale-low 0.1 --scale-high 5.0`**。968 px 幅では画角 1.6′〜80′ に相当するが、
   当時のインデックス（index-4107〜4119 と 5205/5206）が扱えるのは **11′〜2000′** だった。
   下半分は最初から当たりようがなく、探索時間を食うだけだった。
   （インデックス自体は §6.1 で 4′ まで広げ、探索範囲も §6.2 で自動計算にした）
~~4. **星図データが `/mnt/c/AstrometryData` にある**。WSL2 から Windows 側ファイルを
   読むのは非常に遅く、`cpulimit 300` を簡単に超える。~~
   → **これは私の推測で、実測すると誤りだった。** WSL を導入して測ったところ、
   `/mnt/c` からでもキャッシュを消した状態で **2.8 秒**で解ける。§4.4 を参照。

---

## 2. 不具合の一覧

深刻度: **A** = 誤った結果を出す / 動かない、**B** = 動くが失敗しやすい、**C** = 保守性・作法

### A-1. 8bit PNG 変換で星が飽和する（上記）
`convert_to_safe_png()`。→ **v6 では PNG 変換そのものを廃止**。

### A-2. PNG を解かせると、返ってくる WCS の上下が画像と食い違いうる
`solve-field` に PNG/JPEG を渡すと、内部で `image2pnm` → `an-pnmtofits` を通り、
**行の並びが反転した FITS** に対して解かれる。v5 はその `.wcs` を、反転していない
numpy 配列にそのまま当てていた。解けてしまった場合に **矢印が上下逆の位置を指す**、
という「失敗より質の悪い壊れ方」をする。

→ v6 は **星の位置リスト（xylist）**、だめなら **自分で書いた 2 次元 FITS** しか渡さない。
どちらも画像の並び替えが起きないので、原理的にこのズレが起きない。
（実測でも、nova に画像を投げた解と v6 の xylist の解は全点 0.4〜1.9″ 以内で一致し、
上下反転仮説なら出るはずの 800″ のズレは出ない。）

### A-3. オンライン解析のタイムアウトが効かない
```python
with concurrent.futures.ThreadPoolExecutor() as executor:
    future = executor.submit(ast.solve_from_image, ...)
    wcs_header = future.result(timeout=ONLINE_TIMEOUT)
```
`future.result()` は時間で例外を投げるが、**`with` を抜けるところで
`shutdown(wait=True)` が走り、走りっぱなしのワーカーの終了を待つ**。
結果として `ONLINE_TIMEOUT` を過ぎてもセルは止まったままで、
「オフラインへ切り替える」まで到達しない。マニュアル 4.2 に書かれた動作と実装が違う。

→ v6 は `shutdown(wait=False, cancel_futures=True)` を明示。

### A-4. Gaia の照合が「最初の行」を無条件に採用する
```python
gaia_result = v_gaia.query_region(simbad_coord, radius=5 * u.arcsec)
return gaia_result[0][0]['RA_ICRS'], gaia_result[0][0]['DE_ICRS']
```
半径 5″ 以内に複数の Gaia 源があると、**近い順とは限らない先頭行**を掴む。
掩蔽観測のように 1″ を争う用途では致命的になりうる。
→ v6 は分離角が最小のものを選び、候補数と G 等級を表示する。

### A-5. 座標系（フレーム）の取り違え
目標を `frame='fk5'` で作っているが、SIMBAD も Gaia DR3 も **ICRS**。
また `solve-field` の `.wcs` は `EQUINOX=2000.0` しか書かないので
astropy はこれを FK5 と解釈する。実害は 25 mas 程度で小さいが、
**ICRS の値を FK5 と名乗らせている**のは誤り。
→ v6 は `.wcs` に `RADESYS='ICRS'` を明示し、比較の前に必ず ICRS へ揃える。

### A-6. FITS の読み込みが素朴すぎる
```python
data = hdul[0].data
if data is None: data = hdul[1].data
```
- 3 次元（カラー / 動画キューブ）→ 後段の `ny, nx = data.shape` で `ValueError`
- タイル圧縮 FITS（`CompImageHDU`）→ HDU[1] を直接触ると扱いを誤る
- `BLANK` による欠損、`BZERO/BSCALE`、4 次元 `(1,1,ny,nx)` を考慮していない
- FITS 以外は `Image.open` 一本なので、拡張子と中身が食い違うと落ちる

→ v6 は「一番大きい画像 HDU を選ぶ → 2 次元に落とす」を分離し、
カラー面の合成、キューブの平均化、ベイヤー配列の平滑化、`BLANK`/NaN 処理まで面倒を見る。

### B-1. `returncode == 0` でしか `.wcs` を見に行かない
`solve-field` は解けなくても 0 を返し、逆に環境によっては解けても 0 以外を返す。
v5 は 0 以外のとき `.wcs` を一切見ない。
→ v6 は戻り値ではなく **`.wcs` が出来たかどうか**で判定する。

### B-2. 失敗しても一度きり。原因も見えない
`process.stderr` を捕まえているのに一度も表示しないので、
`solve-field: command not found` や「インデックスが無い」がユーザーに届かない。
→ v6 は事前診断（`solve-field` の有無 / `/etc/astrometry.cfg` / index ファイルの列挙）を行い、
失敗時は stderr の末尾と `simplexy` の行を出す。さらに条件を変えて最大 7 回リトライする。

### B-3. WSL のパス変換が手作り
```python
path_no_drive = safe_image_path.split(':', 1)[1]
wsl_path = f"/mnt/{drive_letter}{path_no_drive.replace(os.path.sep, '/')}"
```
`automount.root` を変更している環境、UNC パス、空白入りパスで壊れる。
→ v6 は `wslpath -a -u` に変換させ、コマンドは `shlex.quote` で組み立てる
（`wsl.exe -e bash -lc '...'`）。失敗したときだけ従来式にフォールバックする。

### B-4. 目標の方向を知っているのに使っていない
ユーザーは目標へ向けて撮っている。`--ra/--dec/--radius` を渡せば探索範囲が
全天から半径数度に狭まり、桁違いに速くなる。v5 は常に全天探索していた。
→ v6 は既定で目標座標をヒントに使う（`USE_TARGET_AS_HINT`）。外した場合に備えて
ヒント無しの試行も梯子に残してある。

### B-5. 画角の手がかりを一切使っていない
ヘッダの `FOCALLEN` / `XPIXSZ` / `SECPIX` / 既存 WCS から画素スケールは推定できる。
→ v6 は推定し、`FOCAL_LENGTH_MM` 設定も用意した。推定できたときは ±25% の窓に絞る。
（今回のテスト画像には `FOCALLEN` が無いので、焦点距離を書いておくと効果が大きい。）

### B-6. 手持ちのインデックスで解ける画角かを誰も確かめていない
当時のデータでは 11′ より狭い画角（≒ 焦点距離 1750 mm 超）は**絶対に解けなかった**。
→ v6 は WSL 上の index ファイルを列挙して対応画角を出し、
画像の画角が外れていれば「どの index を足せばよいか」を含めて警告する。
さらに §6.1 で星図データそのものを 4′ まで広げた。

### B-7. `check_internet_connection` が Google を見ている
Google に届いても nova.astrometry.net が落ちていれば意味がなく、
キャプティブポータルは 200 を返す。→ v6 は nova 自身を見る。

### C-1. API キーがソースに直書きされ、公開リポジトリに載っている
`API_KEY = "axvhxwfkvnzreobn"` が GitHub の公開リポジトリと配布 PDF 経由で読める。
**再発行を勧める。** v6 は `os.environ.get("ASTROMETRY_API_KEY", <現行キー>)` にして、
環境変数を入れればコードを触らずに差し替えられるようにした（今のキーでも動く）。

### C-2. 例外の握りつぶし
`except:` / `except Exception:` が 10 箇所以上あり、`pass` や既定値で流している
（`wcs.wcs.cd` が無いときに `scale = 0.00027` を使う箇所など）。
何が起きたか分からないまま、静かに間違った縮尺で描画されうる。
→ v6 は `proj_plane_pixel_scales()` を使い、握りつぶす場所には理由を書いた。

### C-3. 北上画像の CRPIX が 1 画素ずれている
```python
wcs_no_rot.wcs.crpix = [-np.min(new_pix[0]), -np.min(new_pix[1])]
```
FITS の `CRPIX` は 1 始まりなので `1 - min` が正しい。
`shape_out` も切り上げだけで +1 していないため、端が 1 画素欠ける。

### C-4. `plot_original_image` が常に `invert_yaxis()` する
`ROWORDER='TOP-DOWN'`（SharpCap）の画像では正しいが、
一般的な FITS（下から上）では **鏡像**を表示することになる。
→ v6 は `ROWORDER` と入力形式から反転の要否を決める。

### C-5. 一時ファイルを消していない
`tempfile.NamedTemporaryFile(delete=False)` と `*_solver.png` が
実行のたびに `%TEMP%` に溜まる。→ v6 は作業ディレクトリを作って最後に消す。

### C-6. `ipywidgets` 7 系で落ちる
`uploader.value[0]` は 8 系の書式。7 系は辞書なので `KeyError` になる。
→ v6 は両方を受ける。

### C-7. 冗長・軽微
- `except (concurrent.futures.TimeoutError, Exception)` は `Exception` だけで同じ
- `plot_original_image` の `data_safe` を作りながら `vmin/vmax` にしか使っていない
- `INPUT_MODE` に想定外の値を入れても何も言わずに素通りする
- 進捗表示が `print` 直書きで、失敗時に次に何をすべきかが書かれていない

---

## 3. v6 でやったこと

処理の流れ（**星の検出はオンライン・オフラインで共通**にした。同じ入力なら同じ答えが出る）:

```
画像を読む ── FITS(全 HDU / 圧縮 / 3D / 4D / BLANK / BZERO) / PNG / JPEG / TIFF
   ↓        カラーは輝度に合成、動画キューブは平均、ベイヤーは 2x2 平滑化
前処理 ──── 背景面をグリッド中央値で推定して減算
   ↓        ホットピクセル（隣 8 画素の合計 < 中心の 30%）を除去
   ↓        焼き込みオーバーレイ（縁に張り付いた明るい文字列）をマスク
   ↓        ケラレた縁をマスク
星の検出 ── 12σ から 2σ へ順に下げ、12 個以上見つかった時点で確定
   ↓        開口 SNR < 5、伸び > 4 のものは捨てる（文字・光跡・ノイズ対策）
   ↓        重心・フラックス・FWHM を 2 次モーメントで測る
スケール推定 FOCAL_LENGTH_MM / 既存 WCS / SECPIX / FOCALLEN・XPIXSZ / 前回の実測値
   ↓        分からなければ「手持ち index が解ける画角」から探索範囲を自動計算
   ↓
解析 ────── オンライン: 星の位置だけを nova へ送る（画像より桁違いに軽い）
   ↓        オフライン: xylist を solve-field へ。最大 7 段のリトライ梯子
検算 ────── 画素スケールと中心座標が正気か確かめてから描画
   ↓
描画 ────── 撮影時の向き / 北を上・東を左  + 赤経赤緯それぞれのずれ
```

オフラインのリトライ梯子（速くて当たりやすい順、合計 `OFFLINE_TIMEOUT` 秒以内）:

| # | 内容 |
|---|---|
| 1 | xylist + 座標ヒント + スケール既知 |
| 2 | xylist + 座標ヒント + スケール全域 |
| 3 | xylist + 全天 + スケール既知 |
| 4 | xylist + 全天 + スケール全域 |
| 5 | xylist + 全天 + `--no-tweak`（星が少ない画像向け） |
| 6 | 画像 FITS + `--downsample 2` |
| 7 | 画像 FITS + `--downsample 4` |

---

## 4. 検証

### 4.1 テスト画像（`Capture_00001 00_10_33.fits`）

| | v5 | v6 |
|---|---|---|
| 解析に使う星 | 8bit PNG から `simplexy`（**5σ 検出 0 個**） | **17 個**（8σ / 開口 SNR 14〜54） |
| 焼き込み時刻の扱い | 星として拾う | 0〜16 行を自動マスク |
| 画像中心 | — | 04h07m33.94s +41d57m53.14s |
| 画素スケール | — | 1.4616 ″/px（視野 23.58′ × 13.35′） |
| 目標の位置 | — | 画素 (425.8, 243.5)（**画面内**） |
| 中心からのずれ | — | 1.583′（RA 0.918′ 東 / Dec 1.290′ 北） |
| 所要時間 | — | 10.6 秒（オンライン・星リスト送信） |

v6 が作った 17 個の星リストを **本物の astrometry.net エンジン**（nova）に投げると
**1 秒で解け**、画像をそのまま投げた解と全点 0.4〜1.9″ で一致した。
（`solve-field` は WSL がこの PC に入っていないため直接は実行していない。§4.3 参照。）

### 4.2 いろいろな画像で壊れないこと —— 31 ケース全通過

実データ 1 + 合成 30。星を植えた合成画像では、検出位置が植えた位置と
2 px 以内で一致する割合が 80% を超えることも確認している。

```
01 実データ SharpCap 8bit + 焼き込み時刻   17 sources
02 16bit 素直な星野                        45   一致 100%
03 int16 + BZERO=32768                     39   一致 100%
04 float32 + NaN/Inf                       42   一致  98%
05 データが拡張 HDU[1] にある               38   一致  97%
06 タイル圧縮 FITS (CompImageHDU)           42   一致  98%
07 カラー FITS (3, ny, nx)                  43   一致 100%
08 動画キューブ (30 枚) 平均で SNR 回復      40   一致 100%
09 4 次元 (1,1,ny,nx)                       43   一致  98%
10 ベイヤー配列 (BAYERPAT=RGGB)             43   一致 100%
11 上端に焼き込み時刻                       44   一致 100%
12 下端に焼き込み時刻                       45   一致  98%
13 強いかぶり + 周辺減光                    31   一致 100%
14 衛星の光跡 + ホットピクセル 250 個        42   一致  95%
15 星が 6 個しかない                         6   一致 100%
16 天の川級の密集 (1500 星)                400
17 ピンボケ (FWHM 9px)                      42   一致  86%
18 アンダーサンプル (FWHM 1.2px)            44   一致 100%
19 全部飽和した星                           42   一致 100%
20 縁が真っ黒 (ケラレ)                      45   一致 100%
21 8bit グレースケール PNG                  45   一致 100%
22 16bit PNG / 23 カラー JPEG / 24 16bit TIFF     一致 92〜100%
25 一様な画像 (星なし)         → はっきりエラーにする
26 ノイズだけ (曇天)           → 誤検出 0 個（暴走しない）
27 小さい画像 (160x120) / 28 縦長 (400x1200)
29 月明かりの大きなカブリ                   41   一致 100%
30 SIMPLE カードが壊れた FITS  → 誤読せずエラー
31 空白・日本語入りファイル名               44   一致  98%
                                            → 31/31 passed
```

### 4.3 オフライン経路の実機検証（WSL 導入後に実施）

WSL を導入して、**本物の `solve-field` で端から端まで通した**。

環境: Windows 11 26200 / WSL 2.7.12 / Ubuntu 26.04 LTS /
astrometry.net 0.98 / 星図データ 253 ファイル (4′〜2000′) を
`C:\AstrometryData` に配置 (`/etc/astrometry.cfg` の `add_path /mnt/c/AstrometryData`)。

同じ実写画像を 6 通りの形式に変換して、すべて同じ空の場所に解けることを確認した
(`dev/test_wsl_e2e.py`)。基準は nova.astrometry.net の解 (中心 RA 61.891414°,
Dec 41.964760°, 1.4616″/px)。

```
  PASS  実写 FITS 8bit (原本)        3.8 秒 / 星  17 個 / 中心ずれ 0.0″ / 1.4616″/px
  PASS  16bit FITS (BZERO 付き)      3.7 秒 / 星  17 個 / 中心ずれ 0.0″ / 1.4616″/px
  PASS  8bit PNG に変換したもの       3.5 秒 / 星  18 個 / 中心ずれ 0.1″ / 1.4617″/px
  PASS  JPEG (非可逆圧縮)            3.6 秒 / 星  17 個 / 中心ずれ 0.1″ / 1.4617″/px
  PASS  カラー FITS (3, ny, nx)      3.8 秒 / 星  17 個 / 中心ずれ 0.0″ / 1.4616″/px
  PASS  動画キューブ 20 枚            3.9 秒 / 星  17 個 / 中心ずれ 0.0″ / 1.4617″/px
  PASS  上下反転した FITS             3.8 秒 / 星  17 個 / 中心ずれ 0.0″ / 1.4616″/px
                                                                    7/7 passed
```

**A-2 (パリティの取り違え) が実際に起きないことも確認した。**
上下反転した画像は `det(CD)` の符号が正しく反転し (+1.648e-07 → −1.648e-07)、
対応する画素 (原本の (x, y) と反転版の (x, ny−1−y)) が
**すべて 0.00″ で同じ空を指す**。四隅と目標の 5 点で確認した。

ノートブックをそのまま実行した場合:

| モード | 結果 |
|---|---|
| `SOLVE_MODE='OFFLINE'` (座標ヒントあり) | 1 回目の試行で成功 / 17.1 秒 (初回・キャッシュ冷) |
| `SOLVE_MODE='OFFLINE'` (ヒント無し・全天総当たり) | 1 回目の試行で成功 / 5.3 秒 |
| `SOLVE_MODE='AUTO'` | オンラインで成功 / 12.0 秒 |

いずれも解はオンラインと一致した。

### 4.4 「/mnt/c は遅いので WSL 内へコピーすべき」は取り下げた

v6 の初版と、書きかけのマニュアルに
「星図データを WSL 内へコピーすると数倍速くなる」と書いていたが、
**これは測らずに書いた推測で、実測と食い違っていた**ので削除した。

| | 96 MB の読み込み (キャッシュ消去後) | 1 枚の解析 (同) |
|---|---|---|
| `/mnt/c` (Windows 側) | 0.25 秒 | **2.8 秒** |
| ext4 (WSL 内) | 0.03 秒 | — |

生の読み出しはたしかに約 10 倍違うが、`solve-field` が実際に読むのは
星図データのごく一部なので、**解析時間の差はほとんど出ない**。
9.8 GB を二重に持つ価値は無いと判断し、
ノートブックの実行時ヒントからも、マニュアルの推奨からも外した
(`setup_wsl.ps1 -CopyIndexToWsl` は残してあるが、既定では使わない)。

---

## 5. 使い方（v5 からの差分）

`zahyou_v6.ipynb`。セルは 2 つのままなので、マニュアル v2 の手順はそのまま使える。

セル 2 の先頭で設定する項目（増えたもの）:

| 設定 | 既定 | 意味 |
|---|---|---|
| `FOCAL_LENGTH_MM` | `None` | **入れることを強く勧める。** 画角が確定してオフラインが激速になる |
| `SOLVE_MODE` | `'AUTO'` | `'OFFLINE'` にすると自宅でもオフライン経路を試せる |
| `USE_TARGET_AS_HINT` | `True` | 目標の方向を探索の手がかりにする |
| `SEARCH_RADIUS_DEG` | `5.0` | 導入誤差が大きいなら広げる |
| `SHOW_DETECTED_SOURCES` | `True` | 検出した星を緑丸で重ねる（うまく拾えているかの確認用） |
| `IMAGE_PATH` | `None` | ボタンを使わずパス直指定 |

`INPUT_MODE='STAR_NAME'` は、**一度オンラインで引いた名前を記憶する**ので、
次からは山の中でも同じ目標なら使える。

### あわせてやっておくと効く（マニュアル側の話）

1. **星図データを WSL の中へ移す。** `/mnt/c` 読み出しが遅く、`inparallel` で
   1.28 GB を舐めるため `cpulimit` を食い潰しやすい。
   ```
   cp -r /mnt/c/AstrometryData ~/AstrometryData
   sudo sed -i "s#/mnt/c/AstrometryData#$HOME/AstrometryData#" /etc/astrometry.cfg
   ```
   （v6 は `/mnt/` が使われていると実行時にこの案内を出す）
2. **星図データは 4′〜2000′ まで拡張済み**（§6.1）。さらに狭い画角
   （2′〜4′ / ASI290MM で 4830 mm 超）が必要になったら:
   ```
   python dev/fetch_index.py 5201 5200      # +25.4 GB
   ```
   2′ より狭い画角は astrometry.net が配布していないので、
   ビニングやモザイクで画角を稼ぐか、`build-astrometry-index` で自作するしかない。
3. **API キーの再発行。** §C-1。

---

## 6. 任意の画角を「読み込むだけ」で扱えるようにした（追加分）

### 6.1 星図データを 4′ まで拡張した

配布されていたのは `index-4107`〜`4119` と `index-5205/5206` で、**11′〜2000′** しか解けなかった。
これは ASI290MM（センサー 1936×1096 / 2.9µm）なら**焦点距離 1750 mm までが限界**という意味で、
C8（2032 mm）ですら画角 9.6′ で解けない。

`portal.nersc.gov` の 5200 LITE シリーズから **scale 2〜4 を追加取得した**
（`dev/fetch_index.py`。再開・検証つき。7.2 分 / 平均 20 MB/s）。

| index | 画角 | ファイル | 実サイズ | 対応焦点距離 (ASI290MM) |
|---|---|---|---|---|
| `index-5204-00〜47` | 8′〜11′ | 48 | 1.19 GB | 〜2400 mm |
| `index-5203-00〜47` | 5.6′〜8′ | 48 | 2.29 GB | 〜3450 mm |
| `index-5202-00〜47` | 4′〜5.6′ | 48 | 4.53 GB | 〜4830 mm |

**144 ファイルすべて検証済み**（Content-Length 一致 + FITS として開けて `INDEXID` /
`HPNSIDE=2` / `NQUADS>0` が正しい）。`C:\AstrometryData` に配置。
合わせて **4′〜2000′** が連続でカバーされる。

`build-astrometry-index` による自作も検討したが、(1) このツールは WSL の
astrometry.net に同梱されていてこの PC では動かせない、(2) 材料の Gaia カタログ自体が
完成品のインデックスより大きい、(3) 公式ビルドは検証済み — の 3 点から、
**配布済みビルドの取得が正しい選択**と判断した。

### 6.2 コード側: 画角の設定が要らなくなった

1. **探索するスケール範囲を、手持ちの index から逆算する**
   （`blind_scale_bounds()`）。v6.0 は `0.2〜120″/px` 固定だったので、
   **持っていない画角を延々と探し、持っている画角の外側は探せない**という
   両方向の無駄があった。いまは
   「index が対応する画角 ÷ 画像の幅」でその場で計算する。

   | 手持ち index | 幅 968px のときの探索範囲 | 幅 4144px のとき |
   |---|---|---|
   | 11′〜2000′（従来） | 0.614〜136″/px | 0.143〜31.9″/px |
   | **4′〜2000′（追加後）** | **0.223〜136″/px** | **0.052〜31.9″/px** |

2. **機材ごとの画素スケールを覚える**（`remember_scale()` / `remembered_scale()`）。
   鍵は `カメラ名 | 鏡筒名 | 画像サイズ | ビニング | FOCALLEN`。
   一度解ければ、次から同じ機材の画像は**設定を何も書かなくても**
   最初から狭い窓で探せる。SharpCap のように `FOCALLEN` を書かないソフトでも効く。
   ヘッダに焦点距離があるときはそちらを優先する（鏡筒だけ替えた場合に
   古い記憶を掴まないため）。

3. **解けたら焦点距離を逆算して表示する。** テスト画像では 819 mm と出た。
   `FOCAL_LENGTH_MM` に書き写しておけば、以後は一発で確定する。

4. **足りない index を名指しで教える**（`missing_index_advice()`）。
   画角が分かっているのに対応する段が無ければ、必要なファイル名と取得コマンドを出す。
   画角が分からないまま失敗したときも、手持ちの対応範囲を添えて助言する。

検証は §4.3 のシミュレータにケース D / E を追加した。
**D: 0.30″/px（画角 4.8′）の長焦点画像を、設定を一切書かずに 1 回目の試行で解く**（細かい index あり）。
**E: 同じ画像・細かい index なし → 黙って失敗せず、不足している段を説明して終わる。**

### 6.3 再配布用のアーカイブ

利用者が「自分の画角に必要な段だけ」落とせるよう、scale ごとに zip を作った
（`dev/make_index_zips.py`。中身は `AstrometryData/index-....fits` なので
`C:\` 直下に展開すればマニュアル 2.3 の配置になる。段ごとの README と
SHA256SUMS.txt つき）。

---

## 7. VS Code は `jupyter.source_hidden` を無視する（設計変更）

長い解析コードをノートブックに貼ると、画像を選ぶ場所と結果が出る場所が
遠く離れてスクロールが大変になる。そこで cell metadata に
`jupyter.source_hidden` / `collapsed` / `tags: [hide-input]` を入れて
「最初から畳んだ状態」で配ろうとしたが、**VS Code は 3 つとも無視した**
（実機で確認。1500 行がそのまま展開される）。

VS Code の折りたたみ事情も紛らわしい:

| 見た目 | 実際 |
|---|---|
| 実行ボタン **▷** の右の小さな **∨** | **「[デバッグ] セル」メニュー**。折りたたみではない |
| セル左端のマージン | ブレークポイント用のガター |
| セルツールバーの **…** | 切り取り/コピー/結合など。折りたたみ項目は無い |
| 出力左の **⋯** | 「その他の操作」メニュー |

つまり VS Code には「畳んだ状態で配る」手段が無い。
そこで **畳む必要が無いように構造を変えた**:

```
zahyou_v6.ipynb    markdown + 3 セル = 合計 92 行
   セル A 準備          39 行   zahyou_engine.py を読み込む
   セル B 画像を選ぶ      5 行   pick_image()
   セル C 設定と実行     48 行   ★ ここだけ触る。結果もすぐ下
zahyou_engine.py   1569 行   解析の中身 (開かなくてよい)
```

**1640 行 → 92 行。** 画像を選ぶセル B と結果が出るセル C が隣どうしになり、
「行ったり来たり」の要求も同時に満たせた。
代償は配布ファイルが 2 つになること（同じフォルダーに置く必要がある）。
セル A は engine が見つからないとき、探した場所を並べて知らせる。

---

### 7.1 分割後に出た不具合 — 画像選択ボタンが 2 つ出る

`pick_image()` が `return uploader` していたため、`display()` で出した分と、
**セルの最後の式の値として自動表示される分**の 2 つが並んでいた。
戻り値を `None` にして解消。

目で見ないと気づけない種類の不具合なので、
**本物の Jupyter カーネルでノートブックを実行し、出力の mime を数えるテスト**
(`dev/test_notebook_run.py`) を追加した。

```
修正前: セル B の出力 = [display_data(widget), execute_result(widget)]  → 2 個
修正後: セル B の出力 = [display_data(widget)]                          → 1 個
```

---

## 8. まだ手を付けていないこと

- `.ser` / `.avi` の直接読み込み（いまは FITS キューブのみ対応）。
  掩蔽観測の生データが SER なら、対応する価値はある。
- 複数フレームの位置合わせ加算。キューブは単純平均なので、追尾がずれていると星が伸びる。
- 解の**外部検証**（解けた WCS でカタログを引き直して残差を見る）。オンラインなら実装できる。
- index の**部分取得**。48 分割は nside=2 の HEALPix なので、観測する空だけ落とせば
  半分以下で済む。ただし astrometry.net 独自の HEALPix 付番を自前で実装する必要があり、
  取り違えると「特定の方角だけ解けない」という気づきにくい壊れ方をするので見送った。
- GitHub への反映（今回は指示どおり行っていない）。
